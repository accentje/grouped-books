# grouped-books

#### Starting server on local

Run the following commands
```
cd server
npm install
npm run dev
```

#### Starting client side on local

Run the following commands
```
cd client
npm install
npm run dev
```
